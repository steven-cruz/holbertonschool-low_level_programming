#include "holberton.h"
#include <stdlib.h>

/**
 * strtow - splits a string into words..
 * @str: string to split.
 * Return: 1) returns a pointer to an array of strings (words).
 *         2) NULL if str == NULL or str == "".
 */

char **strtow(char *str)
{
	char **p = NULL;

	if (str == NULL || *str == '\0')
		return (NULL);


	return (p);
}
